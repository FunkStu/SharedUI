
  # Shared UI

  This workspace contains the Shared UI code bundle (formerly “Build this”). The original Figma project is available at https://www.figma.com/design/vfhBDBs8bfJ3SUQQGSAO1k/Build-this.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
